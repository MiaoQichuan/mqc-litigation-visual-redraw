# 迭代记录 CHANGELOG

本 skill 遵循语义化版本。

| 日期 | 版本 | 修改内容 | 修改原因 |
| --- | --- | --- | --- |
| 2026-07-09 | 1.0.0 | **修复标题字体真 bug（视觉）**：此前所有渲染器用全局 `<style>text{font-family:黑体}</style>` 设正文字体，而按 CSS 层叠，`<style>` 规则**优先级高于**标题元素上的 `font-family="宋体"` 呈现属性 —— 导致标题被静默重绘成黑体，**在任何渲染器都如此（不止 PNG，master SVG 亦然）**。改为把正文字体挂到根 `<svg font-family=…>` 呈现属性（正文靠继承、标题以「自身属性 > 继承」胜出），统一 4 处生成点（`common.svg_open` + flow/relation/tree）；并在 soffice 专用副本去掉标题描边（描边会让 LibreOffice 把标题转成错字体轮廓）。重渲全部 example 截图；新增回归守卫「禁止全局 `<style> text{}` 盖标题」。回归增至 71 项。 | 标题宋体是本 skill 的核心视觉主张，却被 CSS 层叠静默击穿，例图标题全成黑体（用户发现） |
| 2026-07-09 | 1.0.0 | **开源前文档校正**：README 与实际能力对齐——「四种布局」改为**七种布局**（补齐 `dated_point_timeline` / `relation_tree` / `comparison_table`）、删除并不存在的 **Pillow** 依赖（本项目纯标准库）、示例数「四份」改为**八份**；修复 CHANGELOG 一处**断裂表格行**（两条记录粘连、GitHub 渲染错乱）；新增 `.gitignore`；`last_updated` 与开源日期统一为 2026-07-09。**只改文档，未动任何代码/视觉/能力。** | 此前 README 停留在 4 布局旧口径且误列 Pillow，开源门面必须与代码一致 |
| 2026-07-08 | 1.0.0 | SKILL.md 顶部新增**意图路由表**(按"用户给了什么/想要什么→读哪个规程→选哪个布局"一表定位，含渐进披露:只读所需)与**集中红线禁止表**(禁手摆坐标/禁蓝slate/禁菱形用六边形/禁把论证塞进节点/禁改冻结数值/禁新增图型)。借鉴自外部 skill 的文档组织法(意图路由+Forbidden成表)，只改文档组织、不动任何能力与视觉。加回归守卫防误删。回归增至 70 项。 | 降低使用门槛、帮较弱模型少踩红线(开源友好度+边际质量) |
| 2026-07-08 | 1.0.0 | 新增 `comparison_table` 双栏对比表(A vs B 逐维度横向对读)，确定性自算布局、克制灰红、可选深红整列；用于两裁判要旨/两诉讼方案对比等，解决关系树在两栏多叶时过宽的问题。加示例+3条回归(恰2列/cells完整/lint-clean且比例<2.2)。**范围锁定：本 skill 仅时间轴·流程图·关系图三类(对比表归入关系类的对比变体)，不再扩展其他图型。** | 完成度收尾，并明确不再走新方向 |
| 2026-07-08 | 1.0.0 | **数量级核对(支柱1补洞)**：`audit` 新增 count check——列出抽取的节点/事件/边计数供对照原图；并支持 `provenance.source_count`(提取时记下"原图数了几个")，audit **自动比对**，数量不符即告警"漏读或多读了节点/边"并触发 checkpoint。这是唯一能机器捕捉"我漏读了一个节点"的检查。extraction-guide/schema 增记录规程。新增 3 条回归。回归增至 66 项。 | 提取正确性此前只能靠人核对，数量锚点把"漏读/多读"这类致命错暴露出来 |
| 2026-07-08 | 1.0.0 | **修复 LR 流程连线(真 bug)**：`render_flow` 连线路由原为 TB 写死(源底→目标顶)，LR 图下节点左右排却仍上下连线，导致边绕行、末端悬空/极短。新增完整 **LR 路由**(源右→目标左、母线竖直在左右侧、回边走底部通道)，TB 分支不变。数据流程图悬空短边 N→0、lint clean。新增回归守卫(LR 无悬空短边)。回归增至 63 项。 | LR 从未真正适配，复杂数据流连线翻车 |
| 2026-07-08 | 1.0.0 | **箭头方向防线**(此前零防线)：`audit` 新增"方向审查"——列出**入口节点(无入边)/出口节点(无出边)**与全部 `A→B` 边，并对"无入口/无出口/孤立节点"告警；箭头写反会使该节点从入口/出口名单消失，checkpoint 一眼可查(修法律数据流程图翻车暴露的方向易错问题)。extraction-guide 增"箭头方向"纪律。新增 3 条回归。回归增至 62 项。 | 边 from/to 写反会静默画错、lint 查不出语义反向，用入口/出口分析把方向错误暴露到 checkpoint |
| 2026-07-08 | 1.0.0 | **复杂关系图稳定化(修翻车)**：①`render_tree` 新增严格树自检——多父/环/不可达即**报错并指向 graphviz_relation**，杜绝"把网络画成结构错误的干净树"这一最坏失败；②`render_relation` 跨排边由**对角斜线改为正交**(下-横-下，同排仍水平)，复杂层级型关系网从 10 条 lint 警告→**clean**，且符合"连线正交"既有标准；③extraction-guide 收紧"树 vs 网络"判定(严格层级才用树，任何多父/交叉/环/混合→graphviz_relation，拿不准用 relation)。新增 2 条回归。回归增至 59 项。 | 复杂关系图无法稳定输出的根因是结构误判+关系渲染器画对角，双向修复并加机制兜底 |
| 2026-07-08 | 1.0.0 | **收敛为系统子模块 + 结构优化**：确立 `mqc-litigation-visual` 为系统大名，本 skill 更名 `mqc-litigation-visual-redraw`(只承担"重画"这一块)，全库(SKILL/README/AUTHOR/STANDARDS/schema/tests)统一改名并加系统定位；README 新增仓库结构图与"系统首个模块"说明。结构去重：测试改为直接引用 `examples/` 作单一真相源，删除 7 个重复 fixture(仅保留 edge_* 压力用例)。文档防漂移：STANDARDS 声明"精确数值以各细则/token 为准"。未做过度抽象(不拆包/不建 monorepo)，待第二个模块真正开始时按真实需求再抽公共层。回归 57/57。 | 为"诉讼可视化系统"铺路，同时给旧的过大命名瘦身、降维护漂移 |
| 2026-07-08 | 1.0.0 | **决策节点改形(视觉标准正式变更)**：流程图 decision 由**尖菱形**改为**圆角六边形**(两端切角、6 顶点 r≈2.5 圆角)——菱形是中文的糟糕容器(须撑大×1.5/×1.9、四角浪费且丑)，六边形中段满高矩形装字舒展、仍保留决策信号；`box_size` 去掉浪费撑大，流程图更紧凑；并令六边形与同级步骤块严格等高(合同决策树 1237×756→1151×676)。`hex_inset`/`corner` 提入 style-tokens；flowchart-spec/visual-style/schema/STANDARDS 全部由『菱形』改为『圆角六边形』；新增回归守卫(decision 必为 6 圆角六边形、全图无 4 点菱形)。回归增至 57 项。 | 按用户反馈修掉丑菱形，正式冻结为新标准 |
| 2026-07-08 | 1.0.0 | **支柱一补强·纯文字输入**：extraction-guide 新增"纯文字(判决书/叙述)"特殊源规程——区分事实/主张/『本院认为』推理(只有事实上图、裁判可作深红、推理不做节点)、从行文建主干、保真规则转为"日期/金额/姓名/书名号/法律术语逐字，事件措辞可凝练并记 text_policy: condensed_from_prose"；并加"多图协同"提示(时点链+期间+主体结构应拆成配套多图)。新增回归守卫。回归增至 56 项。 | 补齐律师最高频输入(纯文字判决书→图)这一关键缺口 |
| 2026-07-08 | 1.0.0 | **时间轴选版规则硬化**：extraction-guide 的 Step 1 新增"选版决策阶梯"(按序判定，命中即用)——任一事件无精确可解析日期/事件密集→编号等距版；精确日期且间隔有法律意义→日期比例版；期间→甘特；默认→编号版(最稳)。SKILL/​schema 同步指向该阶梯；新增 2 条回归(dated 遇不可解析日期即报错、阶梯文档在位)。回归增至 55 项。 | 让"用哪种时间轴"由确定规则决定、不靠模型即兴，且无精确日期时安全回落 |
| 2026-07-08 | 1.0.0 | **支柱一（识别·分析·拆解）成型**：确立三支柱定位——读懂丑图(输入)／学工程不改审美／确定性质量地板；`extraction-guide.md` 六步规程(判类型→**先主干**→逐字→剥装饰→定深红→密度取舍+手绘/数字源特则+反模式+自检)接入主流程；`audit.py` 升级为**提取门禁**：红>2 纪律告警、有 uncertainties 或 AI 自选深红即打出 **CHECKPOINT REQUIRED**；新增 5 条 `extraction` 回归。回归增至 53 项。 | 补齐"把乱图读懂拆对"这一封神核心环节，且全程不碰视觉标准 |
| 2026-07-08 | 1.0.0 | **第四个 skill(fireworks-tech-graph)技术复盘**：仅吸收不碰视觉的三条——①**CJK 安全渲染顺序**：cairosvg 会把中文渲成 □ 空框，故降到 soffice 之后（有 soffice 即走中文安全路径，cairosvg 仅兜底）；②lint 增 **XML 合法性**校验；③lint 增 **`url(#id)` 引用完整性**校验。回归增至 48 项。**明确不引入**其模型手写 SVG/坐标提示（ports/corridors/route_points/jump-over）、8 套视觉皮肤+图标+图例、彩色箭头语义，以及超出法律范围的 UML/网络/思维导图等图型。 | 吸收其渲染健壮性经验，拒绝其"模型画图+多彩皮肤"路线与范围膨胀 |
| 2026-07-08 | 1.0.0 | **第二个 skill(diagram-builder)技术复盘**：仅吸收不碰视觉的两条——把"禁蓝/禁 slate"从回归下沉为 lint 拒绝名单（黑名单式，不误伤本项目微冷中性灰）、marker `orient` 卫生检查；核对确认本项目 marker 不存在裁切问题（viewBox 与 path 等宽、userSpaceOnUse 缩放而非裁切），故未retrofit其"markerWidth≥viewBox"告警。**明确不引入**模型手写 SVG/自算坐标、多彩 7 色板/分层配色、图例/主题/Mermaid 外壳。回归增至 47 项。 | 吸收其工程校验、拒绝其"模型画图"路线与彩色外壳 |
| 2026-07-08 | 1.0.0 | **跨 skill 技术加固**（借鉴 Archify，视觉标准不变）：语义地图新增 `schema_version:1`（const，锁定 IR 契约）+ 正式 `schemas/semantic-map.schema.json`（装了 jsonschema 即用，否则回退手写校验）；`validate_map` 报错带元素 id、未知字段告警、dated 无日期即拦；新增**渲染期视觉 lint**（`lint.py`：非有限值/元素越界/对角箭头，只读不改视觉）——顺带修出并修复日期时间轴边缘卡片越界；`render.py` 增 `validate`/`lint` 子命令与 `--strict`。回归增至 46 项。**明确不引入**模型填坐标的 IR、泳道、信息图外壳。 | 吸收其他制图 skill 的工程纪律，同时守住"模型只填语义"的立身之本 |
| 2026-07-08 | 1.0.0 | STANDARDS 声明为单一权威（冲突时以其为准，后定覆盖先定，含接力文件旧标准）；同步 SKILL/schema/rendering 文档口径为"六布局·三时间轴"（对比表尚未加入时的口径）。 | 消除历次迭代累积的文档矛盾 |
| 2026-07-08 | 1.0.0 | 新增**第三种时间轴 `dated_point_timeline`**：按真实日期成比例（诚实刻度）、浅灰长条轴+细竖线分段+年份/年月自适应刻度、无圆点、精确日期入卡、无日期即报错；配长跨度诉讼时效 example。全图统一**标题呼吸区**(+28px)。甘特 `directional` 尾部可平口（示例改平）。 | 补齐"长跨度点时间轴"、修标题贴脸、按需去箭头 |
| 2026-07-08 | 1.0.0 | 新增**流程图整洁标准(flow-std)**：步骤盒统一宽度、连线直上直下优先(仅分叉/汇合走方括号母线、单边贴头小台阶)、内容对称居中且标题正对内容中点；沉淀图2真实流程图为 `ex_flow_parallel` 用例；回归增至 41 项。 | 按真实流程图反馈提升整洁度并冻结为标准 |
| 2026-07-08 | 1.0.0 | 新增**层级树布局 `relation_tree`(主体关系图)**：自算 tidy-tree(叶子等距、父居子中→分叉对称)、方框统一高度+每层统一宽度、方括号 r≈2.5 圆角连线、默认无箭头、层级灰阶深浅；配 example+fixture 与 6 条 `tree-std` 守卫；无需 graphviz。 | 补齐参考图暴露的层级关系图能力缺口 |
| 2026-07-08 | 1.0.0 | **标题改宋体**(方正小标宋简体→思源宋体→华文中宋，绝不落仿宋)；修复 soffice 出 PNG 时标题被替换的字体匹配问题(PNG 副本以已装宋体渲染)；正文仍黑体。 | 诉讼材料标题要公文宋体气质、且开源合规不打包商业字库 |
| 2026-07-08 | 1.0.0 | 修复 `audit.py` 因 `FS["label"]` 键不存在 import 即崩、导致承诺的审计摘要在生产中静默失效；回归覆盖交付主路径。新增中文**禁则排版**(行首不出现收尾标点、逐字不改)。回归增至 31 项。 | 修交付主路径的隐性缺陷、提升排版专业度 |
| 2026-07-03 | 1.0.0 | 首个完整版本：时间轴（时点/期间两形态）、流程图、关系图三大能力冻结；确定性脚本布局（模型只填 JSON、不碰坐标）；SVG+PNG 双交付，无专用渲染器时回落 soffice。 | 建立诉讼可视化重画的完整能力基线 |
| 2026-07-03 | 1.0.0 | 全面自检与硬化：修复中文/空格节点 id 崩溃、非法日期崩溃、graphviz 缺失报错；新增回流边路由、甘特轴自适应、字段默认、分支标签防重叠；新增 `tests/`（25 项回归断言，含几何与美学不变量）。 | 保证高质量与可复现 |
| 2026-07-03 | 1.0.0 | 标题描边强制加粗(soffice回退无粗体也生效)；箭头改固定 10px、节点前留 4px 空档(箭尖不再重合模块)；新增语义地图前置校验器(悬空边/缺字段清晰报错)；文档全面对齐代码(去除过时胶囊/箭头描述)；回归增至 27 项。 | 按反馈修标题与箭头、全面对齐文件、再硬化 |
| 2026-07-03 | 1.0.0 | 撤回偏蓝的 slate 配色，回到中性灰 + 纯色色块；箭头改等腰三角；连线标签去方框(不遮挡连线)；图表标题改微软雅黑加粗；清理死代码与未用导入、dash 统一；回归增至 26 项。 | 修正失败的视觉升级、按用户反馈精调、进一步硬化 |
| 2026-07-03 | 1.0.0 | 视觉对齐 `visual-style-guide` 标准：slate 中性色阶、Linear 缺口箭头、连线加粗 slate、卡片圆角 12、分支标签胶囊化、跨平台字体链、字号层级（标题 30 / 节点 16 / 副 12 / 注 11）。 | 达到"交付极度优雅"的审美要求 |

---

> **把法律画出来 · Make the Law Visible** ｜ 新诉讼可视化 New Litigation Visualization ｜ 缪奇川 出品 ｜ v1.0.0
